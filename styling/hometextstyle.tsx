import { StyleSheet } from "react-native";

const hometextstyles = StyleSheet.create({
    container: {
      flex: 1,
      height: '10%',
      alignItems: 'center',
      justifyContent: 'flex-end',
      paddingBottom: 10, // Adjust paddingBottom as needed
    },
    text: {
      fontSize: 16,
      fontWeight: 'bold',
    },
  });

export default hometextstyles;